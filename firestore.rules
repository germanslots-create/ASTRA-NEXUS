rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /_health_check_/{id} {
      allow read: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

